import { ComingSoon } from '@/components/ComingSoon';

// Multi-user issuer accounts — deferred per docs/blueprint.md §10.
export default function TeamSettingsPage() {
  return <ComingSoon title="Team" phase="a later phase (deferred, docs/blueprint.md §10)" />;
}
