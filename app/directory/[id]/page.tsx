import { ComingSoon } from '@/components/ComingSoon';

// Trainee public profile — docs/build-phases.md Phase 5, second deliverable
// (name, photo, bio, certificate(s), issuer "Approved Issuer" badge,
// open-to-hire state). Scaffolded now so directory cards have somewhere to
// link to; the real page is the next piece of this phase.
export default function TraineeProfilePage() {
  return <ComingSoon title="Trainee profile" phase="Phase 5 (public directory & search) — profile page" />;
}
